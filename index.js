const head   = require('./head');
const tail   = require('./tail');
const middle = require('./middle');
const assertArraysEqual = require('./assertArraysEqual');
const assertEqual = require('./assertEqual');


module.exports = {
  head, 
  tail,
  middle,
  assertArraysEqual,
  assertEqual,
};

const { head } = require('@sjs5953/lotide/index')